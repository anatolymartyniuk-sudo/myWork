История работы над программой блокировки USB-накопителей
========================================================

Расположение: D:\Program Files\PortableGit\USB_BLOCK\

Состав папки (текущий):
- usb_block_tray.cs   - исходник ОСНОВНОЙ программы (трей, C#/.NET 4)
- usb_block_tray.exe  - собранная программа (csc, winexe, трейлер USBICON)
- stop_usb.jpg/.ico   - картинка иконки (трей и значок exe)
- Readme.txt          - инструкция по использованию
- HISTORY.txt         - этот файл
- diag.log/selftest.log - результаты диагностики и автотестов

Запасной вариант (usb_block.ps1/usb_block.cmd/whitelist.txt) УДАЛЁН -
больше не поддерживается (этап 44).

--------------------------------------------------
ИСХОДНАЯ ЗАДАЧА
--------------------------------------------------
Пользователь сообщил: "добавление USB накопителей через whitelist.txt
не работает" - разрешённая флешка в список попадает, но после применения
политики получить к ней доступ нельзя. Дополнительный вопрос: откуда брать
данные по USB-накопителям для файла whitelist.txt, чтобы скрипт работал
корректно.

РАННИЙ ПЕРИОД (этапы 1-28): PowerShell-скрипт usb_block.ps1
============================================================
Запасной инструмент: usb_block.ps1 с меню:
  1 Заблокировать (кроме whitelist), 2 Разблокировать всё USB,
  3 Сканировать и добавить устройства, 4 Обновить правила (+доустановка
  драйверов), 5 Очистить следы USB-накопителей (+ВСЕ из ветки
  "Переносные устройства"), 0 Выход; автоматический режим: -ApplyOnly.

Механика: политика DeviceInstall\Restrictions (DenyUnspecified=1).
В AllowDeviceIDs попадают HardwareID из whitelist + базовые HID-записи
(USB\Class_03, HID_DEVICE), чтобы мыши/клавиатуры продолжали работать.

Ключевой вывод: USB-накопитель — это дерево узлов (USB-узел VID/PID +
USBSTOR-узел диска + том STORAGE\VOLUME). Драйвер диска USBSTOR
добавляется автоматически ТОЛЬКО для подключённых устройств; для
ранее не подключавшихся whitelist-устройств нужен один запуск пункта 4.

Ограничение политики: DenyUnspecified блокирует ТОЛЬКО установку
драйверов НОВЫХ устройств. Уже установленный драйвер политика не
отключает — это ключевое ограничение, из-за которого выбрана другая
механика для основного инструмента (exe).

Кодировки (актуально и для exe): cmd/Readme/HISTORY — UTF-8 без BOM;
ps1/whitelist.txt — UTF-8 BOM (требование PowerShell 5.1 для русского
текста).

Переход на exe (этап 29): вместо установки драйверов выбрано снятие
буквы тома (mountvol <буква>: /p) — накопитель распознаётся Windows,
но не получает букву и остаётся недоступным. exe в трее стал
основным инструментом, ps1 — запасным.

--------------------------------------------------
ЭТАП 29. WHITELIST - БЛОКИ USB+ID+SN+SWD (В РАБОТЕ); РЕШЕНО ДЕЛАТЬ EXE
--------------------------------------------------
Пользователь: "поменяй структуру файла whitelist, заноси в него ID, SWD и SN
разрешённого usb-накопителя. на основе этих данных, если все совпадают,
оборудование должно быть доступно".

Новый формат whitelist.txt - ОДИН БЛОК НА НАКОПИТЕЛЬ из полей:
  USB=USB\VID_xxxx&PID_xxxx     ID USB-узла (разрешает узел)
  ID =USBSTOR\Disk…             конкретный HardwareID диска (модель)
  SN =081NS9HV47JMZLUQ&0        серийный номер накопителя
  SWD=SWD\WPDBUSENUM\…          WPD-узел ("Переносное устройство")
Устройство считается разрешённым ТОЛЬКО при совпадении ВСЕХ полей.
Реальные данные референса (живая система, JetFlash):
  ID =USBSTOR\DiskJetFlashTranscend_8GB___1100
  SN =081NS9HV47JMZLUQ&0
  SWD=SWD\WPDBUSENUM\_??_USBSTOR#DISK&VEN_JETFLASH&PROD_TRANSCEND_8GB&REV_1100#081NS9HV47JMZLUQ&0#{53F56307-B6BF-11D0-94F2-00A0C91EFB8B}
  WPD-узел имеет COMP: wpdbusenum\fs | SWD\Generic (серийник - в пути узла).

Сделано в usb_block.ps1 (этап 29, начато):
1. Новые функции: Read-Whitelist (блочный парсер), Get-SerialRoot,
   Get-UsbVidPidBySerial, Get-WpdBySerial, Test-BlockMatch.
2. Update-AllowList переписан: разрешения СТРОГО из блоков whitelist (USB
   узел + ID диска + STORAGE\VOLUME, если есть диск; + базовые HID-записи
   Class_03/HID_DEVICE). Никакого перечисления подключённых устройств.
   Итого в политике появляются конкретные идентификаторы, а не всё подряд.
3. Disconnect-Unauthorized: разрешает накопитель, только если блок совпал
   по USB+ID+SN+SWD (неполный блок - предупреждение, включается по USB,
   просит дописать ID/SN/SWD).
4. Show-Devices: печатает готовые строки USB=/ID=/SN=/SWD= для ручной
   записи в whitelist.txt.

НЕ ДОДЕЛАНО (пауза, этап 29 продолжаем в следующей сессии):
- Add-ConnectedToWhitelist (пункт 3) всё ещё пишет в whitelist голые
  строки VID/PID старого формата - надо перевести на запись блоков.
- Сам файл whitelist.txt ещё в старом виде (одна строка USB\VID_8564&PID_1000);
  надо переписать в шаблон с блоками.
- Readme.txt не обновлён под новый формат.

ПАРАЛЛЕЛЬНОЕ РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ: "возможно это лучше реализовать не в виде
скрипта, а в виде исполняемого файла, с значком в трее системы".
Выбран вариант: exe в трее = ОСНОВНОЙ инструмент (активный наблюдатель:
следит за подключениями, разрешённый накопитель лечит драйвер автоматически,
чужой - сразу отключает; меню в трее: блокировать/разблокировать/добавить в
whitelist/обновить). Скрипт остаётся ЗАПАСНЫМ способом.
Проверено: csc.exe (.NET Framework 4, C#) присутствует - ЭТО КЛЮЧЕВОЕ
ДЛЯ СБОРКИ EXE. dotnet SDK нет - сборка через csc, target=winexe,
ссылки System.Windows.Forms / System.Drawing / System.Management.
Этап 30 = написать usb_block_tray.cs, скомпилировать, иконка в трее рисуется
кодом (System.Drawing, без .ico файла).

--------------------------------------------------
ТЕКУЩИЙ СТАТУС
--------------------------------------------------
ОСНОВНОЙ ИНСТРУМЕНТ - usb_block_tray.exe (трей). Запасной ps1-вариант
удалён (этап 44).

- Файлы в папке: usb_block_tray.cs (исходник), usb_block_tray.exe (сборка),
  stop_usb.ico, stop_usb.jpg; Readme.txt, HISTORY.txt;
  diag.log, selftest.log (результаты тестов).
- Кодировки: Readme/HISTORY - UTF-8 no BOM.
- Сборка exe (csc .NET Framework 4, target=winexe, иконка -win32icon:
  stop_usb.ico):
    csc -target:winexe -platform:anycpu -win32icon:stop_usb.ico
        -r:System.Windows.Forms.dll -r:System.Drawing.dll
        -r:System.Management.dll -r:System.ServiceProcess.dll
        -out:usb_block_tray.exe usb_block_tray.cs
  После сборки в конец exe добавляется трейлер [jpg][uint32 длина][USBICON]
  (csc его не создаёт) - иконка трея берётся из stop_usb.jpg. В Git Bash
  аргументы /r: подменяются на пути - использовать -r: (с дефисом).
- Механика блокировки (exe): НЕ политика DeviceInstall, а снятие точки
  подключения тома - mountvol <буква>: /p (посторонним накопителям);
  разблокировка - /r + восстановление сохранённых томов
  (mountvol <буква>: \\?\Volume{GUID}\). Учёт снятых томов -
  HKLM\SOFTWARE\USB_Block\UnmountedVolumes ("<путь>|<серийник>|<USB ID>").
  Политика DeviceInstall осталась как наследие и вычищается.
- Whitelist (exe): бинарный, DPAPI LocalMachine + ACL -
  C:\ProgramData\USB_Block\whitelist.dat. Запись = Name + UsbId
  (USB\VID_..&PID_..) + DiskId (USBSTOR\DISK...) + Serial + AddedAt, т.е.
  жёсткая привязка к конкретному накопителю (этап 29, блок USB/ID/SN/SWD).
  Перенос между ПК - .wlb (без пароля) / .wbwe (AES-256+PBKDF2+HMAC).
- Меню трея (этап 43): 1 Заблокировать, 2 Разблокировать,
  3 Добавить устройство, 4 Удалить устройство из whitelist (с блокировкой),
  5 Экспорт whitelist, 6 Импорт whitelist, 0 Просмотр whitelist,
  7 Установить службу мониторинга (SYSTEM), 8 Удалить службу мониторинга,
  9 Удалить программу, Выход. Ключи:
  --diag, --selftest, --makecopies, --service.
- Защищённые копии exe: Program Files\USB_Block и C:\ProgramData\MAA
  (ACL только Administrators/SYSTEM). Обновляются при КАЖДОМ запуске трея
  от администратора (ProtectedCopy.EnsureInstalledCopy, этап 31).
- Запасной ps1 удалён (этап 44): меню (ранний период): 1 Заблокировать,
  2 Разблокировать, 3 Сканировать USB и добавить устройства,
  4 Обновить правила (+доустановка драйверов), 5 Очистить следы
  USB-накопителей (+Переносные устройства), 0 Выход; авторежим
  usb_block.ps1 -ApplyOnly. Механика - политика DeviceInstall\Restrictions.
- История этапа 35 (исправлена кнопка "Добавить"): в UsbQuery были две
  ошибки - некорректный WQL "LIKE 'USB\%'" (в WQL '\' - экранирование,
  запрос падал и глушился) и NormalizeVidPid, резавший id по первому '\'
  (возвращал "USB"). Обе исправлены, добавлен fallback FindUsbIdBySerial
  по реестру для отключённых накопителей.
- Живая система СЕЙЧАС (после этапа 44):
  * exe пересобран (160405 байт), трейлер "USBICON" на месте;
  * --selftest: все 7 проверок OK; --diag: защищённые копии на месте,
    служба мониторинга не установлена, блокировка не активна,
    whitelist.dat не читается без прав администратора (ожидаемо),
    подключённых USB-дисков/томов нет; в реестре остались исторические
    узлы JetFlash (081NS9HV47JMZLUQ, 41KAXI78HFCSUCJF) и другие USBSTOR.
- Открытые задачи (по желанию пользователя):
  * проверка под администратором: установка службы (пункт 7 - запуск от
    имени SYSTEM), удаление службы (пункт 8), включение блокировки,
    добавление устройства в whitelist;
  * после запуска трея от администратора защищённые копии
    (Program Files\USB_Block и ProgramData\MAA) обновятся автоматически
    (ProtectedCopy.EnsureInstalledCopy, этап 31).

--------------------------------------------------
ЭТАП 30. ПРОСМОТР usb_block_tray.cs ПОСЛЕ СБОРКИ EXE
--------------------------------------------------
К этому моменту exe-решение (этапы 29-30) уже было написано и собрано
(usb_block_tray.cs, 3086 строк; usb_block_tray.exe; csc .NET Framework 4,
target=winexe). В текущей сессии выполнен полный просмотр файла:

Состав программы:
- Program.Main: ключи --diag / --selftest / --service / --makecopies,
  повышение прав (UAC) при старте, single-instance mutex, запуск трея.
- ProtectedCopy: защищённая копия exe в "Program Files\USB_Block" и
  "ProgramData\MAA" (ACL: только Администраторы и SYSTEM, S-1-5-11 на
  чтение) - обычный пользователь не может удалить/изменить копию.
- WhitelistStore: whitelist в бинарном ШИФРОВАННОМ виде (DPAPI
  LocalMachine + ACL): C:\ProgramData\USB_Block\whitelist.dat.
  Запись = Name + UsbId (USB\VID_..&PID_..) + DiskId (USBSTOR\DISK...) +
  Serial + AddedAt. Именно жёсткая привязка к конкретному накопителю
  (USB ID + Серийник) - реализация "разрешать только конкретное
  устройство" (этап 29: блоки USB/ID/SN/SWD).
- PortableWhitelist: перенос между компьютерами, файл .wlb:
  * "USBWL" без пароля (бинарный, не текст);
  * "USBWE" с паролем: AES-256-CBC + PBKDF2 (20000 итераций) +
    HMAC-SHA256 (проверка целостности).
- Механика блокировки ИЗМЕНЕНА по сравнению с ps1: вместо политики
  DeviceInstall\Restrictions (которая не действует на уже установленные
  драйверы) - СНЯТИЕ ТОЧКИ МОНТИРОВАНИЯ тома (mountvol X: /p).
  Устройство распознаётся Windows, но не получает букву диска - доступ
  к данным закрыт. Политика DeviceInstall осталась как наследие и
  вычищается при разблокировке (PolicyManager).
- UsbMonitor.Scan: при включённой блокировке перебирает смонтированные
  тома USB (WMI-цепочка DiskDrive->DiskPartition->LogicalDisk) и
  unmount-ит тома, не совпавшие с whitelist (по USB ID + Serial).
- HiddenWindow: невидимое окно ловит WM_DEVICECHANGE (ARRIVAL/REMOVE),
  по событию ускоряет таймер проверки. Плюс фоновый таймер 3 с.
- Уведомления: баллон 8 с + MessageBox с обязательным текстом
  "ДОСТУП ЗАБЛОКОВАНО, ЗВЕРНІТЬСЯ ДО АДМІНІСТРАТОРА" (дедупликация по
  серийнику/метке, привязка к присутствию устройства).
- Служба UsbBlockService (--service): мониторинг без трея, таймер 2 с,
  уведомления в журнал событий. Установка через WMI Win32_Service.Create
  (надёжно с пробелами в пути). Меню трея пункты 7/8.
- Автозапуск: HKLM\...\Run "USB_Block_Tray" -> защищённая копия.
- Меню трея: 1 Заблокировать, 2 Разблокировать, 3 Добавить устройство,
  4 Экспорт whitelist, 5 Импорт whitelist, 0 Просмотр whitelist,
  6 Автозапуск, 7/8 Служба, 9 Удалить программу, Выход.
- Diag (--diag) пишет diag.log; Selftest (--selftest) проверяет
  экспорт/импорт (обычный/парольный/неверный пароль), бинарность
  шифротекста, сериализацию payload, WMI-цепочку диск->том,
  дедупликацию при слиянии whitelist.

Результаты тестов после сборки (живая система, без админа):
- selftest.log: все 7 проверок OK (экспорт/импорт, пароль, сериализация,
  карта томов - 2 тома, слияние whitelist).
- diag.log: админ false, служба/автозапуск/копии не установлены,
  whitelist.dat пуст (0 записей, файла ещё нет), USB-дисков/томов/узлов
  нет; в реестре найдены исторические узлы JetFlash:
    USBSTOR\Disk&Ven_JetFlash&Prod_Transcend_8GB&Rev_1100\081NS9HV47JMZLUQ&0
    USB\VID_8564&PID_1000\081NS9HV47JMZLUQ
- Блокировка НЕ активна (PolicyManager.IsBlocked = false).
- Иконка трея: приоритет - встроенная картинка stop_usb.jpg в трейлере
  exe ([jpg][длина][USBICON]); иначе рисуется запасная кодом
  (System.Drawing, без .ico).

Задачи на продолжение (по желанию пользователя):
- Установка/проверка под администратором: --makecopies (защищённые
  копии), включение блокировки, добавление устройства в whitelist.
- Убедиться, что old ps1 (usb_block.ps1) и новый exe не конфликтуют
  (разные механизмы: Restrictions/узел-диск vs mountvol/том).
- При необходимости перенести в exe недостающее из ps1 (например,
  Install-PresentWhitelisted доустановка драйверов) - пока exe работает
  через снятие буквы тома и в доустановке не нуждается.

--------------------------------------------------
ЭТАП 31. КОПИИ EXE ПРИ КАЖДОМ ЗАПУСКЕ (PROGRAM FILES + PROGRAMDATA\MAA)
--------------------------------------------------
Запрос пользователя: "добавь перемещение защищённой копии исполняемого
файла и в C:\ProgramData\MAA, при отсутствии папки создай её".

Состояние: копия в ProgramData\MAA уже была реализована в
ProtectedCopy.EnsureInstalledCopy() (MaaDir = %ProgramData%\MAA,
MaaExe = usb_block_tray.exe, папка создаётся Directory.CreateDirectory,
ACL такой же, как у Program Files\USB_Block), но вызывалась ТОЛЬКО при:
  * --makecopies;
  * включении автозапуска (AutoStart.Set(true));
  * установке службы (ServiceManager.Install).
При обычном запуске трея копии могли устаревать.

Решение:
- В Program.Main после проверки прав администратора (и до создания
  mutex/трея) добавлен вызов ProtectedCopy.EnsureInstalledCopy().
  Теперь при КАЖДОМ запуске под админом обе защищённые копии
  (Program Files\USB_Block и C:\ProgramData\MAA) обновляются из
  текущего exe, папка ProgramData\MAA создаётся при отсутствии.
- Если запуск уже идёт из защищённой копии - она просто повторно
  копирует себя в обе папки (конструктивно безвредно).

Проверено: пересборка csc (Framework 64 v4.0.30319, target=winexe):
без ошибок; usb_block_tray.exe обновлён (64512 байт).
Примечание об компиляции: в Git Bash аргументы вида /r:... подменяются
на пути, поэтому используются -r: (с дефисом).

--------------------------------------------------
ЭТАП 32. ИКОНКА EXE ИЗ stop_usb.jpg
--------------------------------------------------
Запрос пользователя: "измени значок exe-файла на ./stop_usb.jpg".

Сделано:
- Из stop_usb.jpg (260x280) сгенерирован stop_usb.ico с размерами
  256/48/32/16 (временный C#-помощник jpg2ico.cs, System.Drawing,
  256 - в PNG, остальные в PNG-формате ICO). Файл stop_usb.ico
  сохранён рядом в папке.
- usb_block_tray.exe пересобран csc с -win32icon:stop_usb.ico -
  теперь иконка exe-файла (Explorer/панель задач) из stop_usb.jpg.
- В конец exe добавлен трейлер [jpg][uint32 длина]["USBICON"] со
  stop_usb.jpg - иконка трея тоже берётся из этой картинки
  (AppIcons.TryLoadEmbeddedTrayIcon). Раньше трейлера не было, и
  трей рисовал запасную иконку кодом.

Проверено: сборка без ошибок, --selftest OK, хвост exe = "USBICON".

Примечание: --makecopies/автозапуск/служба копируют exe вместе с
трейлером (копия файла целиком), поэтому иконки сохраняются.

--------------------------------------------------
ЭТАП 33. ПУНКТ 2 "РАЗБЛОКИРОВАТЬ" НЕ ВОЗВРАЩАЛ БУКВУ ДИСКА
--------------------------------------------------
Пользователь: "пункт 1 Заблокировать работает, пункт 2 Разблокировать
не работает - usb-устройство, которое было заблокировано, после
разблокировки не монтируется".

Причина (по справке mountvol):
- Блокировка делала mountvol X: /p. Опция /p "удаляет точку подключения,
  отключает том и ПЕРЕВОДИТ ТОМ В НЕПОДКЛЮЧАЕМОЕ СОСТОЯНИЕ. Том можно
  сделать подключаемым, заново создав точку подключения тома".
- Разблокировка делала только mountvol /r, а /r "удаляет папки и
  параметры реестра точек подключения тома для томов, которые БОЛЬШЕ
  НЕ СУЩЕСТВУЮТ" - т.е. /r НЕ возвращает точки подключения
  существующих (отключённых через /p) томов. Поэтому после
  "Разблокировать" буква не возвращалась.

Решение (внесено в usb_block_tray.cs, класс MountUtil):
- Перед снятием буквы запоминается путь тома: GetVolumePath(letter)
  через "mountvol <буква>:\\ /L" -> "\\?\Volume{GUID}\" (учтён нюанс:
  /L требует форму <буква>:\, а не <буква>:).
- Пары буква->GUID пишутся в реестр
  HKLM\SOFTWARE\USB_Block\UnmountedVolumes (Track()).
- RestoreUnmounted(): для каждой сохранённой записи подбирает свободную
  букву (предпочтительно исходную, FreeLetter()) и заново создаёт точку
  подключения: "mountvol <буква>: \\?\Volume{GUID}\" (Remount()).
- RestoreUnmounted() вызывается при: пункт 2 Разблокировать, добавлении
  устройства (пункт 3), импорте whitelist (пункт 5), удалении программы
  (пункт 9) - всюду, где раньше был только RestoreAll().
- ClearTrack() - очистка учёта (на будущее).

Ограничение: тома, которые залипли под СТАРОЙ версией (до этого фикса),
в реестре не отмечены - их возврат вручную (Управление дисками) или
переподключением накопителя. Новые блокировки восстановимы пунктом 2.

Проверено:
- Отдельный тест парсера "mountvol C:\\ /L" -> "\\?\Volume{GUID}"
  (voltest.cs): OK, remount-аргумент "C: \\?\Volume{...}\".
- Пересборка csc без ошибок (C# 5, без локальных функций).
- --selftest OK.
- exe скопировать сразу не удалось: работал старый экземпляр трея от
  имени администратора (доступ на запись/остановку без прав закрыт).
  После закрытия трея пользователем exe заменён (154261 байт), трейлер
  "USBICON" на месте.

ОБНОВЛЁННАЯ ПАМЯТКА:
- Меню трея: 1 Заблокировать, 2 Разблокировать, 3 Добавить устройство,
  4 Экспорт whitelist, 5 Импорт whitelist, 0 Просмотр whitelist,
  6 Автозапуск, 7/8 Служба, 9 Удалить программу, Выход.
- Блокировка = снятие буквы тома (mountvol /p) посторонним накопителям;
  разблокировка = /r + восстановление сохранённых точек подключения.
- Сборка (из-за пробелов в пути - во временной папке без пробелов):
  csc -target:winexe -platform:anycpu -win32icon:stop_usb.ico
      -r:System.Windows.Forms.dll -r:System.Drawing.dll
      -r:System.Management.dll -r:System.ServiceProcess.dll
      -out:usb_block_tray.exe usb_block_tray.cs
  затем добавить трейлер [jpg][длина]["USBICON"] в конец exe.

--------------------------------------------------
ЭТАП 34. ДОБАВЛЕНИЕ ЗАБЛОКИРОВАННОГО НАКОПИТЕЛЯ В WHITELIST
          С РАЗРЕШЕНИЕМ МОНТИРОВАНИЯ
--------------------------------------------------
Запрос пользователя: "добавь возможность добавления заблокированного
usb-накопителя в whitelist с последующим разрешением монтирования после
добавления".

Суть: заблокированный накопитель (том снят с монтирования, буквы нет)
должен появляться в списке пункта 3, заноситься в whitelist, после чего
его монтирование разрешается (возвращается буква диска).

Что сделано в usb_block_tray.cs:
1. Класс BlockedRecord (Letter, VolumePath, Serial, UsbId) - описание
   снятого с монтирования тома.
2. MountUtil: учёт томов обогащён - запись в
   HKLM\SOFTWARE\USB_Block\UnmountedVolumes теперь хранится как
   "<путь тома>|<серийник>|<USB ID>" (Track()/GetTracked()).
   - GetTracked() - список снятых томов (для показа заблокированных).
   - AllowTracked(serial) - монтирует конкретный накопитель по серийному
     номеру (заново создаёт точку подключения) и убирает его из учёта.
   - WriteTracked() - перезапись учёта; RestoreUnmounted() переписан
     через GetTracked()/ClearTrack().
3. UsbQuery:
   - FindUsbIdBySerial() сделан public;
   - FindDiskIdBySerial() - конкретный HardwareID диска USBSTOR по
     серийнику (в т.ч. когда том не смонтирован).
4. Пункт 3 (DoAddDevice) и новый BuildAddDeviceList():
   - в диалог добавления включаются И подключённые (GetUsbStorages),
     И заблокированные накопители (из учёта; помечаются "(заблокирован)",
     USB ID и HardwareID диска восстанавливаются по серийнику);
   - после сохранения whitelist вызывается RestoreAll + AllowTracked()
     для каждого добавленного (разрешает монтирование); если блокировка
     выключена - RestoreUnmounted() для всех снятых томов;
   - при включённой блокировке затем RunScan() (прочие посторонние
     накопители остаются отключёнными);
   - в итоговом окне показывается число "Разрешено к монтированию".
5. Уведомление о блокировке (NotifyBlockedOnce) дополнено подсказкой:
   "Чтобы разрешить этот накопитель: значок в трее -> 3 Добавить
   устройство."

Проверено: пересборка csc без ошибок; --selftest OK (томов: 3);
exe установлен (156309 байт), трейлер "USBICON" на месте.

--------------------------------------------------
ЭТАП 35. КНОПКА "ДОБАВИТЬ" НЕАКТИВНА (ДВЕ ОШИБКИ В UsbQuery)
--------------------------------------------------
Пользователь: "не могу добавить usb-устройство в whitelist, кнопка
«Добавить» неактивна".

Диагностика (--diag на живой системе с подключённым JetFlash):
- USB-диск находился, но в строке было "USB=" ПУСТО (и VIDPID=USB).
- Кнопка "Добавить" в AddDeviceDialog активируется только если у устройства
  заполнен UsbId/Serial; при пустом UsbId накопитель также молча
  отбрасывается в DoAddDevice.

Причины (обе - в классе UsbQuery, usb_block_tray.cs):
1. FindUsbIdBySerial и GetUsbMassStorageNodes использовали WQL-запрос
   "SELECT * FROM Win32_PnPEntity WHERE DeviceID LIKE 'USB\%'".
   В WQL обратный слэш - служебный символ экранирования, поэтому такой
   запрос некорректен и выбрасывает исключение; оно глушилось catch, и
   функция всегда возвращала null/пусто. Проверено отдельным тестом
   (wmitest.cs): 'LIKE USB\%' - ошибка; 'LIKE USB%' и без WHERE - работают.
2. NormalizeVidPid разрезал id по ПЕРВОМУ обратному слэшу:
   для "USB\VID_8564&PID_1000" head = "USB", далее split по '&' давал один
   элемент - на выходе "USB". ExtractVidPid дополнительно обрезал
   instanceId до первого '\' и передавал в NormalizeVidPid голое "USB".
   (В ps1-версии то же самое делал regex ^USB\(VID_..)&(PID_..).)

Исправления:
- Оба запроса заменены на "SELECT * FROM Win32_PnPEntity" без WHERE;
  фильтрация по префиксу "USB\" выполняется в коде (FindUsbIdBySerial),
  StartWith("USB\VID_") уже был (GetUsbMassStorageNodes).
- FindUsbIdBySerial дополнен fallback-проходом по реестру
  (GetAllMassStorageInstances): находит USB\VID_..&PID_..\SN даже для
  НЕподключённого (заблокированного/снятого) накопителя - нужно функции
  BuildAddDeviceList (этап 34).
- NormalizeVidPid переписан: ищет "VID_", берёт VID_xxxx и PID_yyyy,
  отбрасывая \серийник, &MI_xx, &REV_xx; возвращает
  "USB\VID_xxxx&PID_yyyy" в верхнем регистре (как в ps1).
- ExtractVidPid теперь просто вызывает NormalizeVidPid(instanceId).

Проверено: csc без ошибок; --diag показывает
  USB=USB\VID_8564&PID_1000  SN=081NS9HV47JMZLUQ  DISKID=USBSTOR\DiskJetFlash...
  для подключённого JetFlash; "USB-узлы массовой памяти: Найдено: 1"
  (VIDPID заполнен). --selftest: все 7 проверок OK.
- exe пересобран и установлен (156821 байт), трейлер [jpg][длина]"USBICON"
  на месте (добавлен после сборки, т.к. csc его не создаёт).

Примечание: защищённые копии (Program Files\USB_Block, ProgramData\MAA)
обновляются автоматически при следующем запуске трея от администратора
(ProtectedCopy.EnsureInstalledCopy вызывается при каждом старте, этап 31).

--------------------------------------------------
ЭТАП 36. НОВЫЙ ПУНКТ МЕНЮ: УДАЛЕНИЕ УСТРОЙСТВА ИЗ WHITELIST
          С ПОСЛЕДУЮЩЕЙ БЛОКИРОВКОЙ
--------------------------------------------------
Запрос пользователя: "добавь возможность удаления usb-накопителя из whitelist
с последующей его блокировкой".

Сделано в usb_block_tray.cs:

1. Меню трея переработано - новый пункт сразу после добавления, остальные
   перенумерованы последовательно:
     1 Заблокировать
     2 Разблокировать
     3 Добавить устройство
     4 Удалить устройство из whitelist...   <-- НОВЫЙ
     5 Экспортировать whitelist...
     6 Импортировать whitelist...
     0 Просмотр whitelist...
     ---
     7 Автозапуск вместе с Windows      (был 6)
     8 Установить службу мониторинга    (был 7)
     9 Удалить службу мониторинга       (был 8)
     ---
     10 Удалить программу...            (был 9)
     ---
     Выход
   Обновлён RefreshServiceMenu (текст "9 Удалить службу...") и тексты
   пунктов автозапуска/служб.

2. Новый класс RemoveDeviceDialog - диалог выбора одной записи whitelist
   (ListBox, кнопка "Удалить" активна при непустом списке, при пустом -
   выключена). Формат строки: имя, USB ID, SN, ID диска.

3. Новый метод TrayContext.DoRemoveDevice:
   - берёт текущий whitelist (UsbMonitor.GetWhitelist), при пустом -
     сообщение "удалять нечего";
   - показывает RemoveDeviceDialog, затем подтверждение (Yes/No) с перечнем
     выбранных устройств;
   - исключает выбранную запись по ключу USB ID|SN (EntryKey) и сохраняет
     whitelist (WhitelistStore.Save), вызывает UsbMonitor.Refresh();
   - БЛОКИРУЕТ удалённый накопитель немедленно: перебирает смонтированные
     тома (UsbQuery.GetUsbVolumes) и для совпавших по USB ID/SN вызывает
     MountUtil.Unmount - снятие точки подключения (mountvol <буква>: /p),
     как и общая блокировка; ничего дополнительно включать не нужно;
   - если общая блокировка включена (_blocked) - дополнительно
     PolicyManager.SetBlocked(true) + RunScan (дочищает прочие тома);
   - итоговое сообщение: сколько записей удалено и сколько томов
     заблокировано; если общая блокировка ВЫКЛЮЧЕНА - предупреждение, что
     для постоянной блокировки в будущем надо включить пункт "1";
   - всё в try/catch с текстом ошибки.
   Вспомогательные: VolumeMatchesEntry (совпадение тома с записью),
   DescribeEntry (строка для подтверждения).

Замечание: удаление доступно только в exe-версии (бинарный whitelist.dat).
В запасном ps1/whitelist.txt удаление по-прежнему делается правкой файла.

Проверено:
- csc без ошибок; exe пересобран и установлен (160917 байт), трейлер
  [jpg][длина]"USBICON" на месте;
- --selftest: все 7 проверок OK;
- отдельный тест dlgtest.cs (ссылается на usb_block_tray.exe): диалог
  RemoveDeviceDialog строится, 2 записи в списке, кнопка "Удалить"
  включена; при пустом списке - выключена. DIALOGTEST OK.
- Реальную проверку удаления/блокировки нужно выполнить пользователю:
  whitelist сейчас пуст (0 записей), поэтому удалять пока нечего.

--------------------------------------------------
ЭТАП 37. ЦВЕТ СТАТУСА БЛОКИРОВКИ В МЕНЮ ТРЕЯ
--------------------------------------------------
Запрос пользователя: "измени цвет статуса блокировки: при включенной -
надпись зеленого цвета, при выключенной - красного".

Сделано в usb_block_tray.cs (класс TrayContext):
- Новый метод ApplyStatusColor(): _miStatus.ForeColor = зелёный
  (0,128,0) при _blocked, иначе красный (192,0,0).
- Пункт статуса больше НЕ Enabled=false: у отключённого пункта WinForms
  рисует текст системным серым и ForeColor игнорирует. Оставлен Enabled=true
  (обработчика Click у него нет - нажатие ничего не делает).
- ApplyStatusColor() вызывается в BuildMenu (сразу после создания _miStatus)
  и в RefreshStatus (после каждого изменения состояния блокировки).

Проверено: csc без ошибок; exe пересобран и установлен (160917 байт),
трейлер [jpg][длина]"USBICON" на месте; --selftest - все 7 проверок OK.

--------------------------------------------------
ЭТАП 38. УБРАНА ВОЗМОЖНОСТЬ ЗАПУСКА В КАЧЕСТВЕ СЛУЖБЫ
--------------------------------------------------
Запрос пользователя: "убери возможность запускать программу в качестве
службы".

Что удалено из usb_block_tray.cs:
- Аргумент командной строки --service / -service и ветку
  ServiceBase.Run(new UsbBlockService()) в Program.Main.
- Класс ServiceManager (установка/удаление/запуск/останов службы
  "UsbBlockMonitor" через WMI Win32_Service и ServiceController) - целиком.
- Класс UsbBlockService (ServiceBase, таймер 2 с, уведомления в журнал
  событий) - целиком.
- Пункты меню "8 Установить службу мониторинга" и "9 Удалить службу
  мониторинга", методы DoInstallService/DoRemoveService/RefreshServiceMenu,
  поля _miSvcInstall/_miSvcRemove.
- Ссылки на службу в DoUninstall (пункт "удалить службу") и в Diag.Run
  (строка "Служба установлена").
- using System.ServiceProcess;

Меню после этапа 38 (перенумеровано):
  1 Заблокировать, 2 Разблокировать, 3 Добавить устройство,
  4 Удалить устройство из whitelist..., 5 Экспортировать whitelist...,
  6 Импортировать whitelist..., 0 Просмотр whitelist...,
  7 Автозапуск вместе с Windows, 8 Удалить программу..., Выход.

Программа теперь работает только как трей (или -ApplyOnly-подобные
режимы через --diag/--selftest/--makecopies); служба больше не нужна.

Проверено: csc без ошибок; exe пересобран (155797 байт после трейлера);
трейлер "USBICON" на месте; --selftest - все 7 проверок OK.

--------------------------------------------------
ЭТАП 39. АВТОЗАПУСК: ОТДЕЛЬНЫЕ ПУНКТЫ ДОБАВИТЬ/УДАЛИТЬ
--------------------------------------------------
Запрос пользователя: "добавь удаление программы из автозапуска с ОС".

Было: один пункт-переключатель "7 Автозапуск вместе с Windows" (чекбокс).
Стало (по запросу удаление должно быть явным и отдельным):
  7 Добавить в автозапуск        (активен, если автозапуск выключен)
  8 Удалить из автозапуска       (активен, если автозапуск включён)
  9 Удалить программу...         (сдвинут на один номер)

Сделано в usb_block_tray.cs (TrayContext):
- Поля _miAddAutoStart/_miRemoveAutoStart вместо _miAutoStart.
- Методы DoAddAutoStart() / DoRemoveAutoStart() вместо ToggleAutoStart():
  вызывают AutoStart.Set(true/false), обновляют пункты меню через
  RefreshAutoStartMenu() и показывают баллон "добавлена/удалена из
  автозапуска".
- RefreshAutoStartMenu(): "Добавить" активен только если автозапуск
  выключен, "Удалить" - только если включён (читается по ключу
  HKLM\...\Run "USB_Block_Tray").

Примечание: "9 Удалить программу" (DoUninstall) по-прежнему сам снимает
запись автозапуска, если она была.

--------------------------------------------------
ЭТАП 40. НОВОЕ ОКНО "ДОБАВИТЬ УСТРОЙСТВО" С ТРЕМЯ ПОЛЯМИ;
         ПУНКТ 0 "ПРОСМОТР WHITELIST" ПОКАЗЫВАЕТ ЭТИ ПОЛЯ
--------------------------------------------------
Запрос пользователя: "при добавлении usb-устройства должно создаваться
новое окно с полями: 1 Ім'я Пристрою (заполняет пользователь, по умолчанию
'Новий Пристрій'); 2 Тип Пристрою = ID (заполняет программа, не редактируется);
3 Модель Пристрою = Vendor ID (заполняет программа, не редактируется).
При выборе в меню пункта 0 'Просмотр whitelist' должны отображаться
эти три поля".

Сделано в usb_block_tray.cs:
1. Класс AddDeviceDialog (CheckedListBox множественного выбора) заменён
   на AddDeviceForm - окно добавления ОДНОГО устройства:
   - сверху выпадающий список накопителей (подключённые + заблокированные
     из учёта MountUtil.GetTracked());
   - "1. Ім'я Пристрою" - TextBox, редактируется, по умолчанию
     "Новий Пристрій";
   - "2. Тип Пристрою (ID)" - TextBox ReadOnly, заполняется программой
     из UsbId (USB\VID_xxxx&PID_yyyy);
   - "3. Модель Пристрою (Vendor ID)" - TextBox ReadOnly, заполняется
     программой из BestDiskId (USBSTOR\Disk...) или Model;
   - при выборе устройства поля Тип/Модель заполняются автоматически,
     кнопка "Добавить" активна только при непустом USB ID.
2. DoAddDevice переработан под одно устройство: имя берётся из поля
   "Ім'я Пристрою" (если пользователь оставил пусто/дефолт - сохраняется
   модель устройства); проверка дубликата по USB ID+SN; сохранение,
   AllowTracked/RestoreUnmounted, RunScan - как раньше.
3. WhitelistViewForm.FormatEntry теперь выводит три поля записи:
     Ім'я Пристрою: <Name>
     Тип Пристрою (ID): <UsbId>
     Модель Пристрою (Vendor ID): <DiskId>
     SN: <Serial>
     Метка (ім'я) тома: ...
     Добавлен: ...
   (USB ID/Диск в одну строку заменены на три отдельные строки.)

Формат хранения записи whitelist не менялся (Name+UsbId+DiskId+Serial+AddedAt,
PayLoadVer=2): "Модель Пристрою" отображается из DiskId (там зашиты
VEN_/модель), "Тип Пристрою" - из UsbId.

Итоговое меню трея (этап 39-40):
  1 Заблокировать
  2 Разблокировать
  3 Добавить устройство
  4 Удалить устройство из whitelist...
  5 Экспортировать whitelist...
  6 Импортировать whitelist...
  0 Просмотр whitelist...
  ---
  7 Добавить в автозапуск
  8 Удалить из автозапуска
  ---
  9 Удалить программу...
  ---
  Выход

Проверено: csc без ошибок (Framework 64 v4.0.30319, target=winexe,
-win32icon:stop_usb.ico); exe пересобран, трейлер [jpg][длина]"USBICON"
добавлен после сборки (156821 байт); --selftest - все 7 проверок OK.

--------------------------------------------------
ЭТАП 41. АВТОЗАПУСК БЕЗ ЗАПРОСА UAC (ПЛАНИРОВЩИК ЗАДАЧ)
--------------------------------------------------
Пользователь: "при добавлении программы в автозагрузку, после
перезагрузки программа запрашивает права администратора для внесения
изменений на этом компьютере, такого не должно быть - она должна
запуститься не запрашивая повышения привилегий".

Причина: автозапуск был записью в HKLM\...\Run, которая запускает
программу НЕ повышено; Program.Main при запуске не-админом перезапускал
себя через Start-Process -Verb RunAs -> на каждый вход в систему при
старте из автозапуска появлялся запрос UAC.

Выбор пользователя: "Планировщик задач" - автозапуск через задачу
в Планировщике с "Наивысшими правами". Задачи, созданные с RunLevel
Highest, при входе в систему запускаются службой планировщика молча,
с полными правами и БЕЗ запроса UAC.

Сделано в usb_block_tray.cs (класс AutoStart):
- Автозапуск переведён со Run-ключа на задачу Планировщика:
    schtasks /Create /TN "USB_Block_Tray"
              /TR "\"C:\Program Files\USB_Block\usb_block_tray.exe\""
              /SC ONLOGON /RL HIGHEST /F
  (запуск при входе, с наивысшими правами, без UAC-запроса).
- Set(true) сначала обновляет защищённую копию (EnsureInstalledCopy),
  затем создаёт задачу; Set(false) - schtasks /Delete /F.
- Старый Run-ключ при включении/выключении автозапуска удаляется
  (иначе останется двойной запуск и запрос UAC). Перегрузка с
  ProcessStartInfo (UseShellExecute=false) передаёт аргументы без
  подмены путей, поэтому используется Exec.Run("schtasks.exe", ...).
- IsEnabled(): true, если есть задача ИЛИ остался старый Run-ключ
  (обратная совместимость со старыми установками).
- MigrateRunToTask(): при каждом старте с правами администратора
  (Program.Main, после EnsureInstalledCopy) переводит уже настроенный
  старый Run-ключ на задачу - пользователю не нужно ничего перенастраивать.
- Единственный Mutex исключает двойной запуск (задача + ручной запуск).

Проверено:
- csc (Framework 64 v4.0.30319, target=winexe, -win32icon:stop_usb.ico)
  без ошибок;
- exe пересобран (157333 байт), трейлер [jpg][длина]"USBICON" на месте
  (tail exe: ... d9 8a 22 00 00 55 53 42 49 43 4f 4e);
- --selftest: все 7 проверок OK;
- schtasks /Query /TN "<задача>" возвращает 0, если задача есть, и 1,
  если нет (проверено вручную; /Create требует прав админа - проверит
  пользователь пунктом меню 7).
Примечание: задача создаётся "при входе пользователя, который её создал"
(как /SC ONLOGON без /RU); если на машине логинятся разные пользователи,
каждому из них нужно выполнить пункт 7 для своей сессии.

--------------------------------------------------
ЭТАП 42. УБРАН АВТОЗАПУСК (ПУНКТЫ 7/8 И ПЛАНИРОВЩИК ЗАДАЧ)
--------------------------------------------------
Пользователь: "убираем добавление в автозапуск и создание задачи в
планировщике". Выбран вариант "убрать оба пункта + чистка".

Сделано в usb_block_tray.cs:
- Пункты меню "7 Добавить в автозапуск" и "8 Удалить из автозапуска"
  удалены из меню; "Удалить программу" перенумерован 9 -> 7.
- Класс AutoStart сокращён до одного метода AutoStart.Cleanup(): молча
  удаляет остатки старых версий - задачу Планировщика "USB_Block_Tray"
  (schtasks /Delete) и запись HKLM\...\Run "USB_Block_Tray". Вызывается
  при каждом старте с правами администратора и в DoUninstall. Создание
  задачи (Set(true)) и перевод Run-ключа (MigrateRunToTask) удалены.
- DoAddAutoStart / DoRemoveAutoStart / RefreshAutoStartMenu / поля
  _miAddAutoStart/_miRemoveAutoStart удалены.
- Diag: строка "Автозапуск:" убрана.

Меню после этапа 42:
  1 Заблокировать, 2 Разблокировать, 3 Добавить устройство,
  4 Удалить устройство из whitelist..., 5/6 Экспорт/Импорт whitelist...,
  0 Просмотр whitelist..., 7 Удалить программу..., Выход.

Программа больше не запускается вместе с Windows; ранее созданные
задача/ключ удаляются автоматически при первом запуске от администратора.

--------------------------------------------------
ЭТАП 43. ВОЗВРАТ ЗАПУСКА В КАЧЕСТВЕ СЛУЖБЫ (SYSTEM)
--------------------------------------------------
Пользователь: "возвращаемся к варианту запуска программы в качестве
службы; служба запускается с системной учетной записью; добавляем в меню
вариант установки в качестве службы и удаление службы".

Вернуто в usb_block_tray.cs (отмена этапа 38):
- using System.ServiceProcess;
- Аргумент командной строки --service / -service и ветка
  ServiceBase.Run(new UsbBlockService()) в Program.Main (до проверки прав
  администратора; запускается диспетчером служб, интерфейса нет).
- Класс ServiceManager (имя "UsbBlockMonitor", отображаемое имя
  "USB-блокировка (мониторинг)"):
  * Install(): Win32_Service.Create через WMI - PathName = защищённая
    копия "\"...\USB_Block\usb_block_tray.exe" --service",
    StartMode=Automatic, StartName=null -> СИСТЕМНАЯ учётная запись
    (LocalSystem/SYSTEM); после создания служба запускается.
  * Uninstall(): остановка (ServiceController) + Win32_Service.Delete.
  * IsInstalled(): поиск в ServiceController.GetServices().
- Класс UsbBlockService (ServiceBase): таймер System.Timers.Timer 2 с,
  на каждом тике UsbMonitor.Scan (та же блокировка mountvol /p, что у
  трея); вновь заблокированные накопители пишутся в журнал событий
  (EventLog.WriteEntry, текст + Program.NotifyText).
- Меню трея: "7 Установить службу мониторинга", "8 Удалить службу
  мониторинга" (активность зависит от наличия службы,
  RefreshServiceMenu()); "Удалить программу" сдвинут 7 -> 9.
- DoInstallService / DoRemoveService; DoUninstall дополнительно удаляет
  службу (если установлена) и упоминает её в тексте подтверждения.
- Diag: строка "Служба мониторинга: установлена/не установлена".

Итоговое меню трея (этап 43):
  1 Заблокировать, 2 Разблокировать, 3 Добавить устройство,
  4 Удалить устройство из whitelist..., 5/6 Экспорт/Импорт whitelist...,
  0 Просмотр whitelist..., 7 Установить службу мониторинга,
  8 Удалить службу мониторинга, 9 Удалить программу..., Выход.

Проверено:
- csc (Framework 64 v4.0.30319, target=winexe, -win32icon:stop_usb.ico,
  -r:...,-r:System.ServiceProcess.dll) без ошибок;
- exe пересобран (160405 байт после трейлера), трейлер [jpg][длина]"USBICON"
  на месте;
- --selftest: все 7 проверок OK;
- --diag: служба не установлена (корректно), прочие разделы без ошибок.
- Реальную установку службы (пункт 7) нужно выполнить пользователю с
  правами администратора: служба создастся от имени SYSTEM и начнёт
  мониторинг без трея.

--------------------------------------------------
ЭТАП 44. СЛУЖБА (ПОЧИНКА УСТАНОВКИ), МНОГОПОЛЬЗОВАТЕЛЬСКАЯ БЛОКИРОВКА,
         УДАЛЕНИЕ ЗАПАСНОГО ВАРИАНТА
--------------------------------------------------
Запрос пользователя (3 пункта):
1. Запасной вариант (ps1) больше не ведём.
2. Блокировка не работает, если залогиниться под другим пользователем.
3. При установке службы ошибка "input string was not in a correct format".

ПУНКТ 3 - ПРИЧИНА ОШИБКИ УСТАНОВКИ СЛУЖБЫ:
- В методе Create класса Win32_Service параметры ServiceType и ErrorControl
  по MOF имеют тип uint8 (НЕ string). Код передавал строки:
  inParams["ServiceType"] = "Own Process" - System.Management при назначении
  значения пытается сделать Byte.Parse("Own Process") и бросает
  FormatException ("input string was not in a correct format"), который
  ловился в Install() и показывался как текст ошибки.
  Воспроизведено отдельным тестом (wmisvc_test.cs): стек -
  System.Byte.Parse <- PropertyData.MapValueToWmiValue <- set_Item.
- Исправление в ServiceManager.Install() (usb_block_tray.cs):
  inParams["ServiceType"] = (byte)16;   // 16 = Own Process
  inParams["ErrorControl"] = (byte)1;   // 1 = Normal
  Дополнительно перед созданием службы вызывается
  ProtectedCopy.EnsureInstalledCopy(), чтобы PathName в Win32_Service.Create
  всегда указывал на свежую защищённую копию.
- Проверено тестом в не-админ сессии: FormatException исчез, WMI вернул
  код 2 (0x2 = ERROR_ACCESS_DENIED, ожидаемо для не-администратора);
  под админом установка должна пройти (код 0). Эту проверку выполнит
  пользователь пунктом меню 7.

ПУНКТ 2 - БЛОКИРОВКА ПОД ДРУГИМ ПОЛЬЗОВАТЕЛЕМ:
- Причина: трей работает только в сессии запустившего его пользователя.
  При входе под другим пользователем никого нет, кто снимал бы букву
  с новых/переподключённых накопителей (mountvol /p не переживает
  переподключение устройства), потому блокировка «не работает».
- Решение (уже заложено архитектурой, этап 43): СЛУЖБА мониторинга.
  Она запускается от системной учётной записи (LocalSystem), таймер 2 с,
  UsbMonitor.Scan (тот же механизм mountvol /p, что у трея), уведомления в
  журнал событий. Служба блокирует посторонние накопители для ВСЕХ
  пользователей независимо от того, кто вошёл в систему.
- Все данные общие для всех пользователей: флажок блокировки
  HKLM\SOFTWARE\USB_Block\Blocked, whitelist C:\ProgramData\USB_Block\
  whitelist.dat (DPAPI LocalMachine), учёт снятых томов HKLM (MountUtil).
  Трей и служба работают с одними данными, общая блокировка включается
  один раз (пункт 1) и действует для всех сессий.
- Readme.txt дополнен разделом «Многопользовательский режим / вход под
  другим пользователем»: для контроля при входе другого пользователя
  нужно установить службу (меню -> 7).

ПУНКТ 1 - УДАЛЕНИЕ ЗАПАСНОГО ВАРИАНТА:
- Удалены файлы: usb_block.ps1, usb_block.cmd (запускатель), а также
  старый текстовый whitelist.txt (принадлежал ps1-варианту; exe использует
  бинарный whitelist.dat в C:\ProgramData\USB_Block).
- Readme.txt переписан под exe-версию (механика mountvol, whitelist,
  меню, служба, сборка); упоминания ps1/cmd убраны.
- HISTORY.txt обновлён (состав папки, текущий статус).

Итоговое состояние:
- Единственный инструмент - usb_block_tray.exe (трей); служба - для
  постоянного (многопользовательского) мониторинга.
- exe пересобран, трейлер "USBICON" на месте; --selftest: все 7 проверок OK.
- Открытые задачи (требуют администратора, выполняет пользователь):
  * пункт 7 - установить службу мониторинга (запуск от имени SYSTEM);
  * пункт 8 - удалить службу;
  * проверить блокировку при входе под другим пользователем с установленной
    службой.

--------------------------------------------------
ЭТАП 45. УВЕДОМЛЕНИЯ И ЗНАЧОК В ТРЕЕ ДЛЯ ВСЕХ ПОЛЬЗОВАТЕЛЕЙ;
         УПРАВЛЕНИЕ ТОЛЬКО ДЛЯ АДМИНИСТРАТОРОВ
--------------------------------------------------
Запрос пользователя (3 пункта):
1. После установки службы значок программы должен появляться у ВСЕХ
   пользователей после перезагрузки (сейчас не отображается ни у кого).
2. Уведомления о блокировке USB-накопителей должны получать все
   пользователи.
3. Управление настройками - только для пользователей с правами
   администратора.

ПУНКТ 1 - ЗНАЧОК В ТРЕЕ ДЛЯ ВСЕХ ПОЛЬЗОВАТЕЛЕЙ:
- Добавлен класс TrayTask: задача Планировщика "USB_Block_Tray_Logon",
  срабатывающая при входе ЛЮБОГО пользователя (триггер LogonTrigger,
  группа S-1-5-32-545 = "Пользователи"). Команда - защищённая копия
  usb_block_tray.exe с аргументом --logon.
- RunLevel=Highest: для администраторов задача стартует молча с полным
  токеном (без запроса UAC), для обычных пользователей - с ограниченным
  токеном; пароль не требуется, работает системный LogonTrigger.
- XML пишется во временный файл в кодировке UTF-16 и подаётся
  schtasks /Create /F /XML. Create() вызывается в DoInstallService (меню 7),
  Delete() - в DoRemoveService (меню 8) и в DoUninstall.
- Класс AutoStart.Cleanup() (старая задача "USB_Block_Tray") по-прежнему
  чистит остатки старых версий при старте под администратором.
- XML задачи проверен отдельным тестом (well-formed, GroupId/RunLevel/
  LogonTrigger/Command/Arguments на месте).

ПУНКТ 2 - УВЕДОМЛЕНИЯ ВСЕМ ПОЛЬЗОВАТЕЛЯМ:
- Добавлен класс NotifyStore - общая очередь событий блокировки
  HKLM\SOFTWARE\USB_Block\Events. Значения E<id> в формате
  "id|ticks|serial|label", максимум 32 записи, монотонный номер id,
  дедупликация одного устройства в окне 30 с, XФ ключ доступен НА ЧТЕНИЕ
  любому пользователю.
- Пишут события: служба (UsbBlockService.OnTick при каждом вновь
  заблокированном накопителе) и трей под администратором (RunScan).
- Трей ЛЮБОГО пользователя (в т.ч. без прав) читает очередь и показывает
  не просмотренные этим пользователем события: баллон всегда, окно с
  предупреждением - только для свежих (младше 2 минут); давние события
  (накопившиеся пока пользователя не было) - только баллоном.
- Личный счётчик просмотренных событий хранится в HKCU\Software\USB_Block\
  LastEventId, чтобы один вход не показывал одну и ту же блокировку
  повторно.
- NotifyStore.Write/ReadAll проверены в рамках сборки и selftest.

ПУНКТ 3 - УПРАВЛЕНИЕ ТОЛЬКО ДЛЯ АДМИНИСТРАТОРОВ:
- Program.Main: флаг --logon (-logon). Если трей запущен задачей
  Планировщика, автоповышение прав (re-exec c runas) НЕ выполняется
  (RunLevel у задачи уже делает это для админа, а не-админу повышать не
  дано). EnsureInstalledCopy() и AutoStart.Cleanup() выполняются только
  под администратором.
- TrayContext.BuildMenu: не-администратор получает ОГРАНИЧЕННОЕ меню -
  статус блокировки, неактивный пункт "Управление доступно только
  администраторам", разделитель и Выход. Полное меню (пункты 1-9) -
  только под администратором.
- StatusText для не-админа не показывает число записей whitelist
  (файл недоступен без прав - было бы "Отказано в доступе").
- Трей без прав НЕ сканирует и НЕ блокирует: за блокировку отвечает
  служба; трей без прав только опрашивает очередь уведомлений.
- Текст предупреждения о блокировке: администратору - подсказка
  "значок в трее -> 3 Добавить устройство", остальным - "Управление
  настройками доступно только администраторам".

Меню трея (этап 45, полный набор - только под администратором):
  1 Заблокировать, 2 Разблокировать, 3 Добавить устройство,
  4 Удалить устройство из whitelist..., 5/6 Экспорт/Импорт whitelist...,
  0 Просмотр whitelist..., 7 Установить службу мониторинга,
  8 Удалить службу мониторинга, 9 Удалить программу..., Выход.
Для не-администратора: статус, "Управление доступно только
администраторам" (недоступно), Выход.

Проверено:
- csc (Framework 64 v4.0.30319, winexe, -win32icon) без ошибок;
- exe пересобран (168085 байт после трейлера USBICON), на месте;
- --selftest: все 7 проверок OK; --diag: разделы без ошибок;
- --logon запущен в сессии без прав администратора: процесс стартует и
  живёт (проверено, затем завершён);
- XML задачи Планировщика валиден (проверка парсингом XmlDocument).
- Реальную проверку (появление значка у всех пользователей после
  перезагрузки, уведомления, ограничение управления) выполняет
  пользователь: установить службу (меню 7), перезагрузиться, войти под
  разными пользователями.

--------------------------------------------------
ЭТАП 46. ИСПРАВЛЕНИЕ: schtasks "The task XML contains a value which is
         incorrectly formatted or out of range" (задача значков в трее)
--------------------------------------------------
Запрос пользователя: служба установилась, но задача Планировщика
"USB_Block_Tray_Logon" НЕ создалась - schtasks вернул "The task XML
contains a value which is incorrectly formatted or out of range". Значка в
трее нет после перезагрузки ни у кого, уведомлений тоже никто не получает
(трей просто не запускается - уведомления идёт через него).

ПРИЧИНА (найдена и воспроизведена локально):
- Неверное значение <RunLevel> в XML задачи: было "Highest". В XML-схеме
  Task Scheduler (runLevelType simple type) допустимы ТОЛЬКО значения
  "LeastPrivilege" и "HighestAvailable". Имена "Limited"/"Highest" - это
  значения перечисления PowerShell (New-ScheduledTaskPrincipal -RunLevel)
  и целочисленных констант TASK_RUNLEVEL_*, они НЕ валидны в XML.
  schtasks при этом выдаёт ровно "out of range" (позиция ошибки указывает
  на элемент <RunLevel>).
- Воспроизведено тестом через COM (Schedule.Service):
  * <RunLevel>Limited</RunLevel> или <RunLevel>Highest</RunLevel> ->
    ошибка валидации с позицией (..):RunLevel:...;
  * <RunLevel>LeastPrivilege</RunLevel> -> задача регистрируется;
  * <RunLevel>HighestAvailable</RunLevel> + GroupId + LogonTrigger +
    полные Settings -> единственная ошибка у не-администратора - обычный
    "Отказано в доступе" (0x80070005), т.е. XML валиден, не хватает прав
    (для администратора право есть, задача создастся).
- Кодировка файла XML (UTF-16 LE с BOM) - корректна: schtasks принимает
  UTF-16LE и ANSI.

Исправление в usb_block_tray.cs (TrayTask.BuildXml):
  <RunLevel>Highest</RunLevel>  ->  <RunLevel>HighestAvailable</RunLevel>

Прочие проблемы после этого уходят сами: задача создаётся -> после
перезагрузки у каждого пользователя запускается трей (--logon) -> он
читает очередь HKLM\SOFTWARE\USB_Block\Events и показывает уведомления.

Проверено:
- csc (Framework 64 v4.0.30319, winexe, -win32icon) без ошибок;
- exe пересобран, трейлер USBICON на месте;
- --selftest: 7/7 OK;
- валидность XML подтверждена регистрацией-тестом (COM RegisterTask):
  XML с GroupId + HighestAvailable + LogonTrigger + полными Settings
  проходит проверку схемы.
- Пользователю: в меню НАСТРОИТЬ ЗАДАЧУ ЗАНОВО. Если задача не была
  создана вообще - простая установка службы (меню 7) не пересоздаст её,
  т.к. служба уже стоит; сначала меню 8 "Удалить службу мониторинга",
  затем меню 7 "Установить службу мониторинга" (создастся и служба, и
  задача). После этого - перезагрузка и проверка значков/уведомлений у
  всех пользователей.

--------------------------------------------------
ЭТАП 47. УВЕДОМЛЕНИЯ НЕ ЗАВИСЯТ ОТ ВЫГРУЗКИ ТРЕЯ (ОТДЕЛЬНЫЙ
         ПРОЦЕСС-УВЕДОМИТЕЛЬ --notify)
--------------------------------------------------
Запрос пользователя: не-админ может выгрузить программу из трея ("Выход")
и после этого перестаёт получать сообщения о блокировках. Возможность
выгрузки оставить, но сообщения пользователь должен получать в любом
случае.

Решение: уведомления ВЫНЕСЕНЫ ИЗ ТРЕЯ в отдельный скрытый процесс-
уведомитель, который живёт сам по себе:
- Трей (--logon) больше НЕ показывает уведомления и НЕ опрашивает очередь
  событий: он занимается только блокировкой (под администратором), статусом
  и управлением. Его "Выход" влияет только на сам трей.
- Уведомитель (--notify): невидимый процесс БЕЗ трея, без меню и без
  элементов интерфейса. Запускается задачей "USB_Block_Notify_Logon" при
  входе каждого пользователя, раз в 2,5 с читает общую очередь
  HKLM\SOFTWARE\USB_Block\Events и показывает вновь появившиеся события
  всплывающими окнами (NotifyPopup - без рамки, справа внизу, поверх окон,
  автозакрытие 6 с, очередь событий показываются по одному в столбик).
  Уведомитель нечем "выгрузить" (в системной области его нет), поэтому
  после выгрузки трея сообщения продолжают приходить.
- Взаимодействие: общий счётчик HKCU Software\USB_Block\LastEventId один
  (последнее показанное событие); трей его больше не трогает, единственный
  читатель/писатель - уведомитель, поэтому дублей нет.
- Один экземпляр уведомителя на сессию: мьютекс
  Local\UsbBlockNotify_SingleInstance_v2.

Изменения в usb_block_tray.cs:
- Program.Main: флаг --notify / -notify; до проверки прав выполняется
  ветка: мьютекс уведомителя -> NotifyService.Run() -> выход (без
  повышения прав, без копий, без трея).
- Класс NotifyService: таймер-поллер очереди + очередь сообщений +
  показ по одному NotifyPopup. Помещает окна снизу вверх с отступом.
- Класс NotifyPopup: Form без рамки, атрибуты TopMost + ShowInTaskbar
  false, размер через TextRenderer (работает до отрисовки), автозакрытие.
- TrayContext: TickScan() теперь только RunScan() для администратора;
  методы PollAndNotify()/NotifyEvent() удалены (уведомления ушли в
  NotifyService). Text подсказки меню 7 уточнён.
- TrayTask: две задачи при установке службы:
    USB_Block_Tray_Logon   (--logon)   - значок в трее;
    USB_Block_Notify_Logon (--notify)  - скрытый уведомитель.
  Create() создаёт обе (ошибки по каждой выводятся отдельно текстом
  schtasks с позицией элемента). Delete() удаляет обе. XML задачи
  параметризован аргументом (BuildXml(args)).
- Ограничение (осталось заказчику): процесс уведомителя можно убить через
  Диспетчер задач, но штатным интерфейсом программы (трей/меню) его
  завершить нельзя; после перезагрузки он снова стартует задачей.

Проверено:
- csc (Framework 64 v4.0.30319, winexe, -win32icon) без ошибок;
- exe пересобран, трейлер USBICON на месте; --selftest 7/7 OK; --diag OK;
- --notify запущен в сессии без прав администратора: процесс стартует и
  живёт (проверено, затем завершён);
- --logon и обычный запуск трея не конфликтуют с -notify (мьютексы разные).
- Пользователю: выполняет меню 8 -> 7 (пересоздание обеих задач), после
  перезагрузки проверяет: выгрузить трей у не-админа, затем подождать
  блокировку и убедиться, что сообщение всё равно приходит.

--------------------------------------------------
ЭТАП 48. ОКНО "ДОБАВИТЬ УСТРОЙСТВО": МЕТКА ТОМА В СПИСКЕ, ЦВЕТА
         РАЗРЕШЁННЫХ/БЛОКИРОВАННЫХ, ИМЯ ПО МЕТКЕ
--------------------------------------------------
Запрос пользователя (окно пункта 3 "Добавить устройство"):
1. В списке USB-накопителей убрать DEV ID и SN; вместо них поставить перед
   названием модели Метку Тома накопителя.
2. Цвет шрифта: разрешённые устройства - ЗЕЛЁНЫЙ, блокированные - КРАСНЫЙ.
3. В поле "Ім'я Пристрою" вместо "Новий Пристрій" подставлять Метку Тома
   выбранного для добавления устройства.

Сделано в usb_block_tray.cs:
- StorageDevice: добавлены поля Label (метка тома) и Allowed (в whitelist).
- BuildAddDeviceList: после сбора устройств заполняет Label по серийному
  номеру из смонтированных USB-томов (UsbQuery.GetUsbVolumes) и меток
  томов (UsbQuery.GetVolumeLabels, Win32_LogicalDisk). У отключённых
  ранее заблокированных накопителей метки нет - остаётся пустой.
- AddDeviceForm:
  * список: пункты вида "<Метка>  <Модель>" БЕЗ DEV ID и SN;
  * цвет пунктов через DrawMode.OwnerDrawFixed + DrawItem:
    устройство есть в whitelist (по SN или USB ID) - тёмно-зелёный,
    иначе (блокируется/заблокирован) - тёмно-красный;
  * MarkAllowed(): строит набор ключей "SN:"/"USB:" из WhitelistStore;
  * OnSelection: "Ім'я Пристрою" получает значение = Label выбранного
    накопителя; если метки нет - поле пустое, и при добавлении DoAddDevice
    подставит название модели (существующая логика fallback на sd.Model).

Проверено: csc без ошибок; exe пересобран, трейлер USBICON на месте;
--selftest 7/7 OK.

--------------------------------------------------
ЭТАП 49. УСТРАНЕНИЕ ЛОЖНОГО СРАБАТЫВАНИЯ
         WINDOWS DEFENDER (Behavior:Win32/Persistence.A!ml)
--------------------------------------------------
Программа определялась как Behavior:Win32/Persistence.A!ml - поведенческая
эвристика Defender про "постоянство + самокопирование". Сигнал давала
совокупность типично вредоносных приёмов, которые программа использовала
легально:
  - самокопирование себя в ДВЕ защищённые папки, из которых одна - скрытая
    C:\ProgramData\MAA (самый сильный маркер);
  - создание задач автозапуска через командную строку schtasks.exe /Create
    с XML-файлом во временной папке;
  - отсутствие подписи и явного манифеста приложения.

Что сделано:
1. УДАЛЕНА вторая копия в C:\ProgramData\MAA. Осталась только единственная
   защищённая копия C:\Program Files\USB_Block. Убраны: MaaDir/MaaExe из
   ProtectedCopy, CreateDirectory+копирование, очистка при деинсталляции,
   строка в --diag, комментарии. Служба и задачи/раньше использовали только
   Program Files\USB_Block, так что поведение не изменилось.
2. ЗАДАЧИ ПЛАНИРОВЩИКА регистрируются через COM-интерфейс Schedule.Service
   (Idispatch, отражение), а НЕ через schtasks.exe: вызов schtasks /Create
   из командной строки - характерный признак нежелательного persistence.
   XML построения задачи тот же (группа S-1-5-32-545, HighestAvailable,
   LogonTrigger). Регистрация: ITaskFolder.RegisterTask(name, xml,
   TASK_CREATE_OR_UPDATE, null, null, TASK_LOGON_GROUP(4), null);
   наличие задачи - GetTask (не найдено -> "задачи нет"); удаление -
   DeleteTask. Фолбэк на schtasks.exe сохранён на случай недоступности COM.
3. ДОБАВЛЕН явный манифест приложения app.manifest (asInvoker, uiAccess=false,
   Compatibility Windows 10) - собирается флагом -win32manifest.
4. ИЗМЕНИТЬ ПОВЕДЕНИЕ НЕЛЬЗЯ (без манифеста asInvoker трей/уведомитель не
   смогут работать от обычного пользователя).

ВАЖНО для пользователя: эвристика P.A!ml основана на облачном ML-моделях
Defender, поэтому правки кода снижают риск, но НЕ гарантируют его устранение.
Надёжные способы (выполнять на машине вне этой среды):
  A. Подписать exe доверенным код-signing сертификатом (Authenticode) -
     лучшее средство: файл с доверенной подписью выходит из ML-оценки.
  B. Подать файл на проверку ложного срабатывания в Microsoft:
     https://www.microsoft.com/wdsi/filesubmission (после отправки пакета
     вердикт выходит обычно за 1-2 дня).
  C. До вердикта Microsoft - временное исключение для C:\Program Files\
     USB_Block в Защитнике Windows (Параметры -> Безопасность -> Исключения).

Проверено: csc без ошибок; exe пересобран (манифест встроен), трейлер
USBICON на месте; --selftest 7/7 OK; --diag нормально; COM-путь TrayTask
проверен отдельным зондом: GetFolder(\)/GetTask/RegisterTask/DeleteTask
работают, не-администратору RegisterTask даёт E_ACCESSDENIED (как и
schtasks) - на административном запуске задача создастся.

--------------------------------------------------
ЭТАП 50. НЕ-АДМИН НЕ ПОЛУЧАЛ УВЕДОМЛЕНИЙ: ТРЕЙ ГАРАНТИРУЕТ ЗАПУСК
         ПРОЦЕССА-УВЕДОМИТЕЛЯ
--------------------------------------------------
Жалоба: блокировка работает, значок трея у не-администратора есть, но
сообщений о блокировке не-администратор не получает.

Диагностика (на этой машине, с получением администратора через
Start-Process -Verb RunAs):
1. Права на очередь: HKLM\SOFTWARE\USB_Block\Events - ACL наследуется от
   HKLM\SOFTWARE (BUILTIN\Users: ReadKey, Inherited=True, ContainerInherit),
   т.е. не-админ очередь ЧИТАТЬ может. Не причина.
2. Процесс --notify без прав администратора стартует и живёт (проверено).
3. Сквозной тест: администратором положил тестовое событие E1 в
   HKLM\SOFTWARE\USB_Block\Events, затем не-админом запустил --notify -
   HKCU\Software\USB_Block\LastEventId стал 1 (событие прочитано, попап
   показан). Значит, весь путь уведомления у не-админа ИСПРАВЕН.
Вывод: причина - процесс-уведомитель не ЗАПУСКАЛСЯ при входе. Установки,
сделанные версией до этапа 47, имеют только задачу трея
"USB_Block_Tray_Logon"; задача уведомителя "USB_Block_Notify_Logon" в них
отсутствует, поэтому уведомления не запускались вовсе.

Исправление (чтобы не зависеть от наличия задачи у старых установок):
- Program.StartNotifier(): если мьютекс уведомителя
  (Local\UsbBlockNotify_SingleInstance_v2) ещё не создан (процесс-уведомитель
  не работает), запускает отдельный процесс "usb_block_tray.exe --notify".
  Проверка через Mutex.OpenExisting (WaitHandleCannotBeOpenedException =
  уведомителя нет).
- Program.Main (ветка трея, --logon и обычный запуск): StartNotifier()
  вызывается перед Application.Run(ctx), т.е. уведомитель стартует всегда,
  когда работает трей. Процесс отдельный - "Выход" из трея его не
  останавливает (требование этапа 47 сохраняется). Если задачу выполнил
  планировщик и уведомитель уже работает - второй процесс не создаётся
  (мьютекс).
- TrayTask: IsInstalled()/NotifyInstalled() + общий TaskInstalled(name).
- Diag: строки "Задача трея (--logon)", "Задача уведомителя (--notify)",
  "Процесс-уведомитель", "Событие блокировки (очередь)"; помощники
  NotifyRunning() (по CommandLine процессов) и EventsSummary()
  (записей/макс.id/LastEventId).

Проверено:
- csc без ошибок; exe пересобран, трейлер USBICON на месте;
  --selftest 7/7 OK; --diag выводит новые строки;
- трей "--logon" без прав сам поднял процесс "--notify" (в списке
  процессов два: --logon и --notify);
- если --notify уже запущен, трей второй НЕ создаёт (notify_count=1).
